import React from 'react';
import Header from './Header';

function Features(){
  var testStyle = {
  }
  return(
    <div style={testStyle}>
      <Header/>
      <div>
        <h1>Mic Check</h1>
      </div>
    </div>
  );
}

export default Features;
